# Backlog

| Item | Details | Platform | Priority | Effort |
|---|---|---|---|---|
| Add API versioning | Move the public API under a versioned namespace (`/v1/images`) so breaking changes can be introduced later without coordinating all three clients (web, admin, android) simultaneously. Keep the existing `/api/images` as an alias during transition. Requires changes in `admin/config/routes.rb` and update of the base URL in both clients. | Admin \| Web \| Android | High | Medium |
| Set `RESEND_AUDIENCE_ID` in Vercel | Create an audience in the Resend dashboard and add its ID as an env var. The signup action fails closed without it, so this must be in place before the newsletter feature ships. | Web | High | Small |
| Resolve Vercel config drift | `web/vercel.json` declares a `@vercel/static-build` while the app is `ssr: true` with a working server action, and `@vercel/react-router` is installed but unused in `react-router.config.ts`. Confirm which config the deployment actually honors and delete the misleading one. | Web | Medium | Small |
| Rebase `newsletter-signup` onto main | The branch predates the shared `EMAIL_RE`, contact validation, loader error handling, honeypot, and `WorkImage` changes — `web/app/routes/home.tsx` and `home.test.tsx` will conflict. | Web | Medium | Medium |
| Add honeypot to newsletter signup form | The footer signup form (on the `newsletter-signup` branch) ships in the initial HTML, so it is more bot-exposed than the contact modal. Reuse the hidden "website" field pattern from the contact form. Land with the branch. | Web | Medium | Small |
| Add Sentry error monitoring | Install Sentry SDK in both the web (React Router) and admin (Rails) apps. Capture unhandled exceptions and loader/action errors. Gate the web DSN on `import.meta.env.PROD` so local dev sessions don't pollute the project. Add `SENTRY_DSN` to the env var docs in `README.md`. | Web \| Admin | Medium | Medium |
| Sentry → GitHub issue automation | Configure Sentry to automatically create a GitHub issue when a new error is first seen (Sentry's native GitHub integration supports this via the issue alert rules UI — no code needed). Deduplicated by Sentry so the same error doesn't create duplicate issues. | Web \| Admin | Medium | Small |
| GitHub issue → automated fix via Actions | Set up a GitHub Actions workflow that triggers on new issue creation, uses a Claude-powered agent to analyse the issue and open a draft PR with a proposed fix. Requires the Anthropic API and careful scoping — the agent should open a PR for review, never merge autonomously. | Web \| Admin \| Android | Low | X-Large |
| Set up Firebase App Distribution | Push the APK, testers get an install link and OTA updates with release notes. Free tier, real distribution channel, no Play review. The pick for a handful of testers who shouldn't fiddle with adb. | Android | Medium | Medium |
| Fix image margin asymmetry | The artwork image has a clear left inset (~38px) but minimal or no matching right margin, making it appear off-centre. Right margin should mirror the left. | Android | Medium | Small |
| Reduce whitespace above artwork image | Large gap (~110–130px) between the "contact" link and the top of the artwork image leaves the top third of the screen sparse. Tighten the spacing so the image sits closer to the header. | Android | Medium | Small |
| Add bottom padding to footer | "Alan Mulhall \| © 2026" sits too close to the Android gesture navigation pill. Add sufficient bottom padding to clear the system UI on all device sizes. | Android | Medium | Small |
| Add swipe affordance to image slider | Pagination shows "01 / 15" but there is no visual cue that the gallery is swipeable. Add arrows, dot indicators, or a swipe-hint animation so first-time users discover the gesture. | Android | Low | Small |
| Increase "contact" link tap target | The underlined "contact" text uses a small monospaced font with no extra padding, making it difficult to tap reliably. Increase the vertical (and optionally horizontal) touch target area. | Android | Low | Small |
| Fix grey text contrast across the app | All secondary text (contact, caption, pagination, footer) uses medium-light grey on the off-white background at ~2.5–3:1 contrast — fails WCAG AA. Darken the `Muted` token to at least 4.5:1 for small text. A single token change fixes all instances. | Android | High | Small |
| Reduce gap between image and caption | The caption text sits ~80–90px below the bottom of the artwork, making it feel disconnected from the image it describes. Tighten to ~16–24dp so the caption reads as part of the same block. | Android | Medium | Small |

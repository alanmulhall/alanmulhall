cd web && npx lint-staged

STAGED_RB=$(git diff --cached --name-only --diff-filter=d | grep -E '^admin/.*\.rb$' | sed 's|^admin/||')
if [ -n "$STAGED_RB" ]; then
  cd admin && bundle exec rubocop --format github $STAGED_RB
fi

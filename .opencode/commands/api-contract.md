---
description: Check API contract matches across admin/web/android
agent: build
---

Load the `api-contract` skill and follow its instructions to verify the API response shape is in sync across all three clients.

Read the admin controller, web loader and types, and Android DTO and domain model. Report any field mismatches, nullability issues, or normalization differences.
